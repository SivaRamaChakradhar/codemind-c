#include<stdio.h>
int main()
{
    int a,Volume,Surface_area;
    scanf("%d",&a);
    Surface_area=6*a*a;
    Volume=a*a*a;
    printf("Surface area = %d",Surface_area);
    printf(" and Volume = %d",Volume);
    
}